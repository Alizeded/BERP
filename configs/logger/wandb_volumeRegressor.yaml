# https://wandb.ai

wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  # id: acoustic-toolkit-volumeRegressor-ParametricPredictor-Gammatone # training on Gammatone model
  # id: acoustic-toolkit-volumeRegressor-ParametricPredictor-Mel # training on Mel model
  id: acoustic-toolkit-volumeRegressor-ParametricPredictor-MFCC # training on MFCC model
  # id: acoustic-toolkit-volumeRegressor-evaluation-Gammatone # evaluate on Gammatone model
  # id: acoustic-toolkit-volumeRegressor-evaluation-Mel # evaluate on Mel model
  # id: acoustic-toolkit-volumeRegressor-evaluation-MFCC # evaluate on MFCC model
  anonymous: null # enable anonymous logging
  # project: volumeRegressor_ParametricPredictor_Gammatone
  # project: volumeRegressor_ParametricPredictor_Mel
  project: volumeRegressor_ParametricPredictor_MFCC
  # project: volumeRegressor_evaluation_Gammatone
  # project: volumeRegressor_evaluation_Mel
  # project: volumeRegressor_evaluation_MFCC
  entity: ${your_wandb_entity}
  log_model: True # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  # entity: "" # set to name of your wandb team
  group: ""
  tags: []
  job_type: ""
