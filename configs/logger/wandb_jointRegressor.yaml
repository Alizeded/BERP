# https://wandb.ai

wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-Gammatone-wightedSampler # training on joint Gammatone model
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-Mel-weightedSampler # training on joint Mel model
  id: acoustic-toolkit-jointRegressor-MFCC-mixRealdata-weightedSampler # training on joint MFCC model
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-Spectrogram-weightedSampler # training on joint Spectrogram model
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-SimRIR-MFCC # training on joint MFC model with Sim RIR data
  # id: acoustic-toolkit-jointRegressor-MFCC-mixRealdata-weightedSampler_4RAPs # training on joint MFCC model with 4 params
  # id: acoustic-toolkit-jointRegressor-MFCC-mixRealdata-weightedSampler_2RAPs # training on joint MFCC model with 2 params
  # id: acoustic-toolkit-jointRegressor-MFCC-mixRealdata-weightedSampler_2RAPs_unrelated # training on joint MFCC model with 2 params with unrelated RAPs
  # id: acoustic-toolkit-jointRegressor-MFCC-mixRealdata-weightedSampler_hybrid_abl # training on joint MFCC model with hybrid data
  # id: acoustic-toolkit-jointRegressor-RoomEncoder-MFCC-mixRealdata # training on joint MFCC model with room encoder
  # id: acoustic-toolkit-jointRegressor-baselineFullCNN-mixReal # training on joint baseline FullCNN model
  # id: acoustic-toolkit-jointRegressor-baselineFullCNN-mixReal-10params # training on joint baseline FullCNN model
  # id: acoustic-toolkit-jointRegressor-baselineCRNN-mixReal # training on joint baseline CRNN model
  # id: acoustic-toolkit-jointRegressor-baselineCRNN-mixReal-10params # training on joint baseline CRNN model
  # id: acoustic-toolkit-jointRegressor-baselineREnet-mixReal # training on joint baseline REnet model
  # id: acoustic-toolkit-jointRegressor-baselineREnet-mixReal-10params # training on joint baseline REnet model
  # id: acoustic-toolkit-jointRegressor-baselineTAECNN-mixReal # training on joint baseline TAECNN model
  # id: acoustic-toolkit-jointRegressor-baselineCNNMLP-mixReal # training on joint baseline CNNMLP model
  # id: acoustic-toolkit-jointRegressor-baselineCNNMLP-mixReal-10params # training on joint baseline CNNMLP model
  # id: acoustic-toolkit-jointRegressor-baselineCNNMLP-mixReal # training on joint baseline CNNMLP model
  # id: acoustic-toolkit-jointRegressor-evaluation-Gammatone # evaluate on Gammatone model
  # id: acoustic-toolkit-jointRegressor-evaluation-Gammatone-mixReal # evaluate on Gammatone model with real data
  # id: acoustic-toolkit-jointRegressor-evaluation-Gammatone-Real # evaluate on Gammatone model with synthetic data
  # id: acoustic-toolkit-jointRegressor-evaluation-Gammatone-Syn # evaluate on Gammatone model with synthetic data
  # id: acoustic-toolkit-jointRegressor-evaluation-Mel-mixReal # evaluate on Mel model
  # id: acoustic-toolkit-jointRegressor-evaluation-Mel-Real # evaluate on Mel model with real data
  # id: acoustic-toolkit-jointRegressor-evaluation-Mel-Syn # evaluate on Mel model with synthetic data
  # id: acoustic-toolkit-jointRegressor-evaluation-MFCC-mixReal # evaluate on MFCC model
  # id: acoustic-toolkit-jointRegressor-evaluation-MFCC-Real # evaluate on MFCC model with real data
  # id: acoustic-toolkit-jointRegressor-evaluation-MFCC-Syn # evaluate on MFCC model with synthetic data
  # id: acoustic-toolkit-jointRegressor-evaluation-Spectrogram-Real # evaluate on Spectrogram model
  # id: acoustic-toolkit-jointRegressor-evaluation-Spectrogram-Syn # evaluate on Spectrogram model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineFullCNN-Syn # evaluate on baseline FullCNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineFullCNN-Real # evaluate on baseline FullCNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineCRNN-Syn # evaluate on baseline CRNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineCRNN-Real # evaluate on baseline CRNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineREnet-Syn # evaluate on baseline REnet model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineREnet-Real # evaluate on baseline REnet model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineTAECNN-Syn # evaluate on baseline TAECNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineTAECNN-Real # evaluate on baseline TAECNN model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineCNNMLP-Syn # evaluate on baseline CNNMLP model
  # id: acoustic-toolkit-jointRegressor-evaluation-baselineCNNMLP-Real # evaluate on baseline CNNMLP model
  anonymous: null # enable anonymous logging
  # project: RAP_JointRegressorParametricPredictor_Gammatone_Huber_mixRealdata
  # project: RAP_JointRegressorParametricPredictor_Mel_Huber_mixRealdata
  project: RAP_JointRegressor_ParametricPredictor_MFCC_Huber_MixRealdata
  # project: RAP_JointRegressorParametricPredictor_Spectrogram_Huber_mixRealdata
  # project: RAP_JointRegressorParametricPredictor_SimRIR_MFCC
  # project: RAP_JointRegressor_ParametricPredictor_MFCC_mixRealdata_noFullRAPs
  # project: RAP_JointRegressor_ParametricPredictor_MFCC_mixRealdata_hybrid
  # project: RAP_JointRegressor_RoomEncoder_MFCC_mixRealdata
  # project: JointRegressorbaselineFullCNN
  # project: JointRegressorbaselineCRNN
  # project: JointRegressorbaselineREnet
  # project: JointRegressorbaselineTAECNN
  # project: JointRegressorbaselineCNNMLP
  # project: JointRegressor_evaluation_Baseline
  # project: JointRegressor_evaluation_Gammatone
  # project: JointRegressor_evaluation_Mel
  # project: JointRegressor_evaluation_MFCC
  # project: JointRegressor_evaluation_Spectrogram
  # project: JointRegressor_evaluation_Gammatone_BUTRetrans
  # project: JointRegressor_evaluation_Mel_BUTRetrans
  # project: JointRegressor_evaluation_MFCC_BUTRetrans
  # project: JointRegressor_evaluation_baseline_BUTRetrans
  entity: lucx
  log_model: False # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  # entity: "" # set to name of your wandb team
  group: ""
  tags: []
  job_type: ""
