# https://wandb.ai

wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  # name: "" # name of the run (normally generated by wandb)
  save_dir: "${paths.output_dir}"
  offline: False
  id: acoustic-toolkit-jointRegressor-ParametricPredictor-Gammatonegram # training on joint Gammatone model
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-Melspectrogram # training on joint Mel model
  # id: acoustic-toolkit-jointRegressor-ParametricPredictor-MFCC # training on joint MFCC model
  # id: acoustic-toolkit-jointRegressor-evaluation-Gammatone # evaluate on Gammatone model 
  # id: acoustic-toolkit-jointRegressor-evaluation-Mel # evaluate on Mel model
  # id: acoustic-toolkit-jointRegressor-evaluation-MFCC # evaluate on MFCC model
  anonymous: null # enable anonymous logging
  # project: jointRegressor_ParametricPredictor_Gammatonegram
  # project: jointRegressor_ParametricPredictor_Melspectrogram
  project: jointRegressor_ParametricPredictor_MFCC_Xpos
  # project: jointRegressor_evaluation_Gammatone
  # project: jointRegressor_evaluation_Mel
  # project: jointRegressor_evaluation_MFCC
  entity: lucx
  log_model: True # upload lightning ckpts
  prefix: "" # a string to put at the beginning of metric keys
  # entity: "" # set to name of your wandb team
  group: ""
  tags: []
  job_type: ""
